<Window x:Class="DetectorSismos.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bienvenido a Detector de Sismos" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20">
            <StackPanel>
                <TextBlock Text="🌍 Detector de Sismos" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Sistema de Monitoreo en Tiempo Real" 
                           FontSize="16" 
                           Foreground="#ECF0F1" 
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Contenido -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20">
            <StackPanel>
                <TextBlock Text="¡Bienvenido!" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Margin="0,10,0,10"/>
                
                <TextBlock TextWrapping="Wrap" 
                           FontSize="14" 
                           Margin="0,0,0,15">
                    Esta aplicación te permite monitorear sismos en tiempo real desde cualquier parte del mundo utilizando datos oficiales del Servicio Geológico de Estados Unidos (USGS).
                </TextBlock>

                <TextBlock Text="✨ Características principales:" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Margin="0,10,0,10"/>

                <StackPanel Margin="15,0,0,0">
                    <TextBlock TextWrapping="Wrap" Margin="0,5">
                        <Run Text="🔍 " FontSize="16"/>
                        <Run Text="Detección de sismos en tiempo real" FontWeight="SemiBold"/>
                        <LineBreak/>
                        <Run Text="   Consulta automática de datos actualizados de USGS"/>
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="0,5">
                        <Run Text="📊 " FontSize="16"/>
                        <Run Text="Información detallada" FontWeight="SemiBold"/>
                        <LineBreak/>
                        <Run Text="   Magnitud, ubicación, profundidad, coordenadas GPS"/>
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="0,5">
                        <Run Text="🔔 " FontSize="16"/>
                        <Run Text="Notificaciones automáticas" FontWeight="SemiBold"/>
                        <LineBreak/>
                        <Run Text="   Alertas para sismos con magnitud ≥ 4.5"/>
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="0,5">
                        <Run Text="🌊 " FontSize="16"/>
                        <Run Text="Alertas de tsunami" FontWeight="SemiBold"/>
                        <LineBreak/>
                        <Run Text="   Indicador especial para sismos con riesgo de tsunami"/>
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="0,5">
                        <Run Text="⏰ " FontSize="16"/>
                        <Run Text="Monitoreo automático" FontWeight="SemiBold"/>
                        <LineBreak/>
                        <Run Text="   Actualización cada 5 minutos (opcional)"/>
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="0,5">
                        <Run Text="📅 " FontSize="16"/>
                        <Run Text="Múltiples períodos de consulta" FontWeight="SemiBold"/>
                        <LineBreak/>
                        <Run Text="   Última hora, día, semana o mes"/>
                    </TextBlock>
                </StackPanel>

                <Border Background="#FFF9E6" 
                        BorderBrush="#F39C12" 
                        BorderThickness="2" 
                        CornerRadius="5" 
                        Padding="15" 
                        Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="⚠️ Requisitos:" 
                                   FontWeight="Bold" 
                                   FontSize="14"
                                   Margin="0,0,0,5"/>
                        <TextBlock TextWrapping="Wrap" FontSize="12">
                            • Conexión a Internet activa
                            <LineBreak/>
                            • Windows 10 o superior
                            <LineBreak/>
                            • Aproximadamente 200 MB de espacio libre
                        </TextBlock>
                    </StackPanel>
                </Border>

                <TextBlock TextWrapping="Wrap" 
                           FontSize="12" 
                           Foreground="Gray"
                           Margin="0,15,0,0"
                           TextAlignment="Center">
                    Los datos provienen del Servicio Geológico de Estados Unidos (USGS)
                    <LineBreak/>
                    Esta aplicación es gratuita y de código abierto
                </TextBlock>
            </StackPanel>
        </ScrollViewer>

        <!-- Botones -->
        <Border Grid.Row="2" Background="White" BorderBrush="#BDC3C7" BorderThickness="0,1,0,0" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1"
                        x:Name="btnCancelar"
                        Content="Salir"
                        Width="100"
                        Height="35"
                        Margin="0,0,10,0"
                        Background="#95A5A6"
                        Foreground="White"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="btnCancelar_Click"/>

                <Button Grid.Column="2"
                        x:Name="btnContinuar"
                        Content="Comenzar"
                        Width="120"
                        Height="35"
                        Background="#27AE60"
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="14"
                        Cursor="Hand"
                        Click="btnContinuar_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
