<Window x:Class="DetectorSismos.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cargando..." Height="220" Width="440"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">
    <Border Background="{StaticResource BgCardBrush}" 
            BorderBrush="{StaticResource BorderSubtleBrush}" 
            BorderThickness="1" CornerRadius="12"
            Padding="32,28">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.15" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Border Width="40" Height="40" CornerRadius="8" Background="{StaticResource PrimaryDarkBrush}" Margin="0,0,16,0">
                    <TextBlock Text="SAS" FontSize="12" Foreground="White" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="SASMEX · Centro de Alertas Sísmicas" 
                           FontFamily="{StaticResource AppFont}" FontSize="16" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Grid.Row="1" x:Name="txtMensaje" Text="Cargando recursos del sistema..." 
                       FontFamily="{StaticResource AppFont}" FontSize="13" 
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16"/>
            <ProgressBar Grid.Row="2" x:Name="progressBar" Height="22" 
                         IsIndeterminate="True"/>
        </Grid>
    </Border>
</Window>
